import Register from "../Register";
import Header from "../Header";

export default function UserEdit(props) {
  return (
    <div>
      <Header headerText={"Edit User Info"} />
      <Register />
    </div>
  );
}
